PORT=3000
DATABASE_PATH=./data/roster-calculator.sqlite

# Docker host port. The app still uses port 3000 inside the container.
# Defaulting to 8081 keeps it next to LOHC Flows on 8080.
ROSTER_PORT=8081
